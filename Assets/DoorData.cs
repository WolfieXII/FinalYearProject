using UnityEngine;

// Logic for setting the type of door for the door objects the agent will be opening (Red, Green or Blue).
public class DoorData : MonoBehaviour 
{
    public KeyType doorType;
}
